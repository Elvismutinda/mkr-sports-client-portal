import { CustomHeader } from '@/components/CustomHeader'

function page() {
  return (
    <CustomHeader
      title="Fixtures"
      subtitle="Upcoming matches and events"
    />
  )
}

export default page